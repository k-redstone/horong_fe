import React from 'react'
interface SignupProps {
  setStep: React.Dispatch<React.SetStateAction<number>>
}
function SignupTerms({ setStep }: SignupProps) {
  return <div>SignupTerms</div>
}

export default SignupTerms
